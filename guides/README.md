# Rotas dos guias

Os guias usam `../?guide=<id>` e âncoras como `#rotation`. Um único shell HTML renderiza qualquer registro de `data/registry.js`. Isso evita duplicar páginas e funciona em subdiretórios do GitHub Pages sem regras de rewrite.
